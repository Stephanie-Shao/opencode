# OpenCode "打开项目"界面重构需求文档

**版本**: v1.0  
**日期**: 2026-02-27  
**需求方**: Zoey  
**文档编写**: Zoe (AI编排层)

---

## 1. 项目背景与目标

### 1.1 背景
OpenCode 原有"打开项目"界面面向开发者设计，对非技术用户（编剧）不够友好。需要针对剧本创作场景重新设计。

### 1.2 目标
- 为编剧提供简洁直观的剧本项目管理界面
- 保持与原生 OpenCode 一致的视觉风格
- 以弹窗形式呈现，而非完整页面

---

## 2. 用户画像

### 2.1 目标用户
- **身份**: 编剧、剧本创作者
- **技术水平**: 非技术人员，不熟悉开发工具
- **使用场景**: 短剧剧本创作
- **核心诉求**: 简单、直观、专注于创作

---

## 3. 功能需求

### 3.1 核心功能
| 功能 | 说明 | 优先级 |
|------|------|--------|
| 展示用户所有剧本 | 以列表/卡片形式展示当前用户的所有剧本项目 | P0 |
| 创建新剧本 | 提供新建剧本入口，仅需输入名称 | P0 |
| 切换剧本 | 点击剧本项目后进入该项目 | P0 |
| 展示用户信息 | 弹窗头部展示当前用户基本信息 | P1 |

### 3.2 不包含的功能
- ❌ 创作进度显示
- ❌ 剧本类型区分（电影/电视剧/短剧）
- ❌ 嵌套文件夹/目录层级
- ❌ 复杂的项目管理功能（删除/重命名等可后续迭代）

---

## 4. 页面流程

> 弹窗有两个触发入口，但呈现内容和行为完全相同：
> - **入口 A**：登录后初始界面的"打开项目"按钮
> - **入口 B**：进入原生 OpenCode 界面后，最左侧侧边栏顶部的 **"+"号**（打开项目）

```
用户登录 OpenCode
    ↓
系统自动分配沙盒环境（Volume 持久化数据）
    ↓
用户点击"打开项目"按钮（入口 A）
或 用户在编辑界面点击左侧边栏"+"号（入口 B）
    ↓
┌──────────────────────────────────────┐
│           弹窗界面打开                 │
│  ┌────────────────────────────────┐  │
│  │ 用户信息区                       │  │
│  ├────────────────────────────────┤  │
│  │                                  │  │
│  │  📁 剧本项目一                   │  │
│  │  📁 剧本项目二                   │  │
│  │  📁 剧本项目三                   │  │
│  │                                  │  │
│  ├────────────────────────────────┤  │
│  │  [+ 新建短剧剧本]                │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
    ↓
用户选择：
    ├─→ 点击现有剧本 → 进入该剧本（原生OpenCode界面）
    └─→ 点击新建剧本 → 输入名称 → 创建并进入
```

---

## 5. 详细页面需求（PRD）

### 5.0 登录界面改动要求

> ⚠️ 登录界面当前存在两个功能相近的按钮：【剧本项目】和【打开项目】，需做如下处理：

| 按钮 | 处理方式 | 说明 |
|------|----------|------|
| 【剧本项目】 | **移除** | 与【打开项目】功能重复，删除该按钮 |
| 【打开项目】 | **保留并修改行为** | 点击后打开 **本文档 5.2 节定义的自定义弹窗**，而非原生 OpenCode 的打开项目界面 |

- **关键约束**：登录界面【打开项目】触发的弹窗必须是自定义版本（5.2节），不能调起原生 OpenCode 的文件夹选择器或任何原生弹窗。

---

### 5.1 弹窗规格
| 属性 | 要求 |
|------|------|
| 形式 | 模态弹窗（Modal Dialog） |
| 尺寸 | 宽度 600-800px，高度自适应（最大不超过屏幕80%） |
| 位置 | 屏幕居中 |
| 背景 | 半透明遮罩（rgba(0,0,0,0.5)） |
| 关闭方式 | 点击遮罩/点击关闭按钮/按 ESC 键 |

### 5.2 页面结构

```
┌─────────────────────────────────────────────┐
│  👤 用户名                        [×]      │  ← 头部：用户信息 + 关闭按钮
├─────────────────────────────────────────────┤
│                                             │
│  📁 项目名称一                               │  ← 剧本列表区
│     /workspace/项目名称一            │
│                                             │
│  📁 项目名称二                               │
│     /workspace/项目名称二                    │
│                                             │
│  📁 项目名称三                               │
│     /workspace/项目名称三                    │
│                                             │
├─────────────────────────────────────────────┤
│                                             │
│  [ + 新建短剧剧本 ]                          │  ← 底部操作区
│                                             │
└─────────────────────────────────────────────┘
```

### 5.3 各区域详细需求

#### 5.3.1 头部区域
- **用户信息**: 显示当前登录用户名称/头像（如有）
- **关闭按钮**: 右上角 × 按钮，点击关闭弹窗

#### 5.3.2 剧本列表区
- **展示形式**: 垂直列表，每个项目一行
- **项目信息**:
  - 图标：📁 文件夹图标
  - 名称：剧本项目名称
  - 路径：完整路径（灰色小字，可选显示）
- **排序**: 按最后修改时间倒序（最新的在上面）
- **点击交互**: 点击整个行进入该剧本
- **空状态**: 如果没有剧本，显示"暂无剧本，点击下面创建"

#### 5.3.3 新建剧本区
- **按钮文案**: "+ 新建短剧剧本"
- **按钮位置**: 底部居中或底部左侧
- **点击交互**: 
  1. 弹出输入框（或表单），要求输入"剧本名称"
  2. 点击确认后，在 `/workspace/scripts/` 下创建同名文件夹
  3. 自动进入该剧本项目

### 5.4 新建剧本流程

```
点击"+ 新建短剧剧本"
    ↓
弹出子对话框/输入框
    ↓
输入框提示："请输入剧本名称"
    ↓
用户输入名称，点击"创建"
    ↓
后台创建文件夹 `/workspace/{剧本名称}`
    ↓
自动打开该剧本（进入原生OpenCode界面）
```

---

## 6. 数据模型

### 6.1 文件系统结构
```
/workspace/                    # 沙盒工作目录（直接作为剧本项目根目录）
├── 项目一名称/                # 剧本项目1（文件夹）
│   └── ...                   # 项目文件
├── 项目二名称/                # 剧本项目2
│   └── ...
└── 新项目名称/                # 新建的项目
    └── ...
```

### 6.2 关键约束
- **扁平结构**: 所有剧本项目直接在 `/workspace/` 下，不能嵌套
- **文件夹形式**: 每个剧本项目是一个文件夹
- **命名唯一**: 同一用户下剧本名称不能重复
- **Volume持久化**: 依赖 Daytona Volume 保证数据不丢失

---

## 7. 与OpenCode集成方案

### 7.1 集成范围
- **仅替换**: "打开项目"界面（原 `home.tsx` 或对应路由）
- **入口点**（共两处，行为完全一致）：
  1. 用户在登录后初始界面点击"打开项目"按钮时触发弹窗
  2. 用户在原生 OpenCode 编辑界面内，点击最左侧侧边栏顶部的 **"+"号**（打开项目）时触发弹窗
- **后续流程**: 选择剧本后，进入原生 OpenCode 编辑界面，不做任何修改

### 7.2 技术边界
- 不修改 OpenCode 核心编辑功能
- 不修改文件系统API
- 仅修改/新增UI层面的组件

### 7.3 未来扩展（本需求范围外）
- 剧本项目同步到 GitHub
- 剧本删除/重命名功能
- 剧本模板功能

---

## 8. 界面风格要求

### 8.1 视觉一致性
- **必须保持**: 与原生 OpenCode 一致的配色、字体、圆角、阴影
- **不能自定义**: 独立的视觉风格或配色方案
- **建议做法**: 复用 OpenCode 现有的 UI 组件库（@opencode-ai/ui）

### 8.2 参考风格元素
- 主色调：沿用 OpenCode 品牌色
- 按钮样式：使用 OpenCode 原生 Button 组件
- 字体：系统默认字体栈（-apple-system, BlinkMacSystemFont 等）
- 间距：遵循 OpenCode 的 spacing 规范

---

## 9. 非功能需求

### 9.1 性能
- 弹窗打开速度 < 500ms
- 剧本列表加载 < 1s（假设项目数 < 50）

### 9.2 兼容性
- 支持 Chrome/Safari/Firefox/Edge 主流浏览器
- 响应式：适配不同屏幕尺寸（弹窗宽度自适应）

### 9.3 安全性
- 剧本名称需要过滤特殊字符（防止路径遍历）
- 用户只能访问自己的 `/workspace/` 目录

---

## 10. 验收标准

### 10.1 功能验收
- [ ] 登录界面**不存在**【剧本项目】按钮
- [ ] 登录界面【打开项目】点击后打开的是自定义弹窗（5.2节），而非原生 OpenCode 弹窗
- [ ] 弹窗形式正确呈现，无页面跳转
- [ ] 正确展示所有剧本项目列表
- [ ] 点击剧本项目能正确进入
- [ ] 新建剧本功能正常工作
- [ ] 新建后自动进入新剧本
- [ ] 在原生 OpenCode 界面点击左侧边栏"+"号，能触发相同的弹窗（入口 B 可用）

### 10.2 视觉验收
- [ ] 与原生 OpenCode 风格一致
- [ ] 弹窗尺寸、位置符合要求
- [ ] 无样式错乱

### 10.3 数据验收
- [ ] 剧本数据正确保存到 Volume
- [ ] 下次登录数据不丢失
- [ ] 多设备登录数据同步正常

---

## 11. 附录

### 11.1 术语表
| 术语 | 说明 |
|------|------|
| 沙盒 (Sandbox) | Daytona 提供的云端隔离开发环境 |
| Volume | 持久化存储卷，保证数据不随沙盒重启丢失 |
| 原生OpenCode | 未经修改的OpenCode编辑器界面 |
| 弹窗 (Modal) | 覆盖在当前页面上方的对话框 |

### 11.2 参考资料
- OpenCode 原始 UI 组件库: `@opencode-ai/ui`
- Daytona SDK 文档: https://daytona.io/docs
- 原生 OpenCode 界面截图: (需补充)

---

## 12. 变更记录

| 日期 | 版本 | 变更内容 | 作者 |
|------|------|---------|------|
| 2026-02-27 | v1.0 | 初始版本 | Zoe |
| 2026-02-28 | v1.1 | 补充入口 B：原生 OpenCode 界面左侧边栏"+"号也触发相同弹窗 | Zoey |
| 2026-02-28 | v1.2 | 登录界面移除【剧本项目】按钮；明确【打开项目】触发的是自定义弹窗而非原生弹窗 | Zoey |

---

**文档状态**: 待确认  
**下一步**: Zoey 确认需求后，交给执行层 Agent 实现